#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Ensure builds work for affected projects
pnpm nx affected:build --uncommitted

# Run E2E tests on affected projects
pnpm nx affected:e2e --uncommitted

# Run Storybook tests on affected projects
pnpm nx affected --target=test-storybook --uncommitted